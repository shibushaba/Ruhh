import 'package:flutter/material.dart';

abstract final class NBColors {
  static const black = Color(0xFF0A0A0A);
  static const shadow = Color(0xFF080808);
  static const offWhite = Color(0xFFF5F0E8);
  static const darkBg = Color(0xFF121212);

  static const budget = Color(0xFF22C55E);
  static const habit = Color(0xFFF97316);
  static const prayer = Color(0xFF14B8A6);
  static const movie = Color(0xFFA855F7);
}

abstract final class NBMetrics {
  static const borderWidth = 3.0;
  static const radius = 4.0;
  static const shadowOffset = Offset(4, 4);
}
